name: Pruebas Unitarias EcoLogistics
on: [push, pull_request] # Se ejecuta en cada subida de código

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar código
        uses: actions/checkout@v3

      - name: Configurar PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'

      - name: Ejecutar Pruebas Unitarias
        run: |
          # Aquí se ejecuta el comando que verifica tus algoritmos
          # Ejemplo: php tests/PruebaRutas.php 
          echo "Ejecutando pruebas sobre el algoritmo de rutas..."